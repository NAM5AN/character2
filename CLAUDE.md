# CHA LAB 작업 규칙

## 접근성

자동 검사(`npm run lint`)가 잡아주는 것과 사람이 직접 확인해야 하는 것이 다르다.
아래 두 가지는 규칙으로 잡히지 않으니 릴리스 전에 손으로 확인한다.

### 릴리스 전 키보드 완주

마우스를 쓰지 않고 끝까지 갈 수 있어야 한다. 중간에 막히면 그 화면은 배포하지 않는다.

1. `Tab` 한 번 → "본문 바로가기"가 나타나고, `Enter` 로 본문에 도달한다
2. 프로필 입력 → 이름·공개 프로필까지 `Tab` 만으로 채운다
3. AI 추론 검수 → 맞음/애매함/아님을 `Tab` + `Enter` 로 고른다
4. 20문항 인터뷰 → 특히 아래 세 유형을 확인한다
   - 양극 척도: `Tab` 한 번으로 그룹에 들어가고, 좌우 화살표로 이동·선택된다
   - 순위형: 드래그 없이 위/아래 버튼만으로 순서를 바꿀 수 있다
   - 관계별 반응표: 각 행의 선택지에 도달할 수 있다
5. 요약 리포트 → 카드를 `Enter` 로 펼친다
6. 상세 리포트 → **섹션 아코디언이 `Enter` 로 열린다** (이전에 여기서 완전히 막혔다)
7. 이용 코드 모달 → `Escape` 로 닫히고, `Tab` 이 모달 밖으로 나가지 않으며,
   닫은 뒤 포커스가 열었던 버튼으로 돌아온다

포커스가 지금 어디 있는지 **항상 눈에 보여야 한다.** 안 보이면 그 자체가 결함이다.

### 새 위젯을 만들 때

**`role` 을 선언하면 그 역할의 키보드 규약도 함께 구현한다.**

`role="radiogroup"` 을 붙이면 보조기술은 사용자에게 "화살표로 고르세요"라고 안내한다.
그런데 화살표 핸들러가 없으면, 안내받은 조작법이 통하지 않아 **역할 선언이 없는 것보다 나쁘다.**
실제로 이 저장소의 양극 척도가 그 상태였다.

구현할 자신이 없으면 역할을 선언하지 말고 평범한 버튼 그룹으로 두는 편이 낫다.

### 캐릭터 색을 다룰 때

포인트 색은 오너의 캐릭터 머리·눈동자 색에서 나오므로 **어떤 값이든 들어올 수 있다.**

- **색조(hue)는 바꾸지 않는다.** 캐릭터의 색이라는 사실이 남아야 한다.
- 대비는 명도로만 확보한다. `deriveThemePalette` 가 배경과 그 위 글자색을 한 쌍으로 계산한다.
- accent 위의 글자색은 `--character-accent-ink` 를 쓴다. 흰색·검정을 직접 박지 않는다.
- 이 약속은 `test/theme-contrast.test.mjs` 가 지킨다. 대비뿐 아니라 색조가 틀어지는지도 검사한다.

## 검증

`npm run lint` · `npx tsc --noEmit` · `npm test` · `npm run build` 를 통과해야 한다.
GitHub Actions 의 `Verify` 워크플로가 push 와 PR 에서 같은 검사를 돌린다.

린트 경고는 빌드를 막지 않지만 늘어나면 안 된다. 특히 `jsx-a11y/*` 경고가 새로 생겼다면
그건 키보드나 스크린리더 사용자가 쓸 수 없는 요소를 방금 추가했다는 뜻이다.
