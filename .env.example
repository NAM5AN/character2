# Optional overrides. Production uses Vercel AI Gateway OIDC automatically.
OPENAI_MODEL=openai/gpt-5.6-luna
ANTHROPIC_MODEL=anthropic/claude-sonnet-5

# Supabase defaults to the existing shorts project + character2_* RPC namespace.
# These are optional overrides; the publishable key is safe for client/server distribution.
SUPABASE_URL=https://kfgtvifupumjuewwxzmz.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_KROAv1c1eX3wlEt8Mog8OQ_jNTMJzoM

# Server-only secret used to authenticate write-only AI telemetry RPCs.
# Generate a long random value and never expose it through NEXT_PUBLIC_* variables.
CHARACTER2_TELEMETRY_SECRET=

# Optional extra salt for rate-limit IP hashing.
RATE_LIMIT_SALT=
